#include <iostream>
#include <string>
using namespace std;

// Note: Units digit of a number is the digit in the one's place of the number.
int main() {
	for(long n = 0; n < 5; ++n) {
		long N, P, D; // [1..10^15]
		cin >> N >> P >> D;
		string ns = to_string(N);
		//
		// TO_DO: Complete your solution here...
		//
		cout << ns << endl;
	}
	return 0;
}
